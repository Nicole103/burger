<div class="mt - 4">
    <h4 class="devoured-burger-name">{{burger_name}}</h4>
    <button type="button" class="btn btn-dark" data-id="{{this.id}}">
        <i class="fas fa-trash-alt"></i>
        Trash It!
    </button>
</div>